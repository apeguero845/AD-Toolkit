# SDD Configuration — AD macOS Integration
# Auto-generated by sdd-init

context: >
  macOS Active Directory integration tooling for CISA (César Iglesias S.A.).
  Covers domain join automation and AD password change with local account sync,
  targeting cesariglesias.local domain with strict GPO policies (LDAPS required).

strict_tdd: false
test_runner: null
test_layers:
  unit: false
  integration: false
  e2e: false

phase_rules:
  proposal: explore -> intent -> scope -> approach
  spec: requirements -> scenarios -> constraints
  design: architecture -> components -> data-flow
  tasks: breakdown -> estimates -> dependencies
  apply: task -> verify -> commit
  verify: spec-match -> test-run -> report
  archive: delta-sync -> final-state
