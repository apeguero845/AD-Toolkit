# SDD Configuration — AD macOS Toolkit (CISA)
# Auto-generated by sdd-init — 2026-06-12

context: >
  macOS Active Directory integration tooling for CISA (César Iglesias S.A.,
  República Dominicana). Native SwiftUI app with GSS.framework (Kerberos),
  SMAppService + XPC privileged helper tool, dsconfigad, and sysadminctl.
  Covers domain join automation, AD password change with local account
  and keychain sync. Targets cesariglesias.local domain, macOS 13+.

strict_tdd: false
test_runner: null

test_layers:
  unit: false
  integration: false
  e2e: false

# Testing capability detail
testing:
  runner: null
  frameworks: []
  coverage: false
  quality:
    linter: false
    type_checker: false
    formatter: false
  layers:
    unit:
      available: false
      tool: null
      notes: "macOS app, no test target configured"
    integration:
      available: false
      tool: null
      notes: "Requires AD environment (KDC, LDAP)"
    e2e:
      available: false
      tool: null
      notes: "Requires bound Mac + AD server"

# Detected stack
stack:
  type: macOS-native
  language:
    - Swift (SwiftUI)
    - C (GSS bridge)
    - Objective-C (bridging header)
  build_system: XcodeGen (project.yml → .xcodeproj)
  min_deployment: macOS 13.0
  bundle_id: com.cisa.ad-toolkit
  architecture: 3-tier
  tiers:
    - SwiftUI App (UI, no privileges)
    - GSS Bridge (C wrapper for GSS.framework Kerberos)
    - Privileged Helper Tool (SMAppService + XPC, runs as root)
  key_frameworks:
    - GSS.framework (Kerberos password change)
    - Security.framework (Keychain sync)
    - OpenDirectory.framework (local account sync)
  external_tools:
    - dsconfigad (domain join/leave)
    - sysadminctl (local password sync)
    - security CLI (keychain sync fallback)
  ci: null
  repo_host: null

# Session preflight (from sdd-init)
session:
  pace: interactive
  artifacts: openspec
  prs: ask-always
  review_budget: 400

phase_rules:
  proposal:
    - Include rollback plan for risky changes
  spec:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "cd ad-toolkit && ./setup.sh + Xcode build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas
